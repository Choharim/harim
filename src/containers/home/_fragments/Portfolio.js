import React from "react";
import styled from "styled-components";

const Portfolio = () => {
  return <Container id="portfolio">pf</Container>;
};

export default Portfolio;

const Container = styled.section`
  display: flex;
  width: 100%;
  height: 100vh;
  scroll-snap-align: center;
`;
